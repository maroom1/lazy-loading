import { NewItemDirective } from './new-item.directive';

describe('NewItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NewItemDirective();
    expect(directive).toBeTruthy();
  });
});
